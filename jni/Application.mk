# Uncomment the following line to build all android architectures, or
# change to architectures required (armeabi armeabi-v7a x86 mips). You
# will also need to add the relevent copies of libsonivox.so to the
# NDK.

APP_ABI := all
